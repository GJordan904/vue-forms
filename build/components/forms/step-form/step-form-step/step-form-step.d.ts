import { Vue } from 'vue-property-decorator';
import { Step } from '../step';
export declare class StepFormStep extends Vue {
    step: Step;
    id: string;
    mounted(): void;
}
export default StepFormStep;
